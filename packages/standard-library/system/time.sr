package System;

extern fn sleep(milliseconds: int32): void;

module Time {

    /**
     * Pauses the execution of the current thread for the specified duration.
     * @param milliseconds The duration to sleep in milliseconds.
     */
    fn Sleep(milliseconds: int32): void {
        sleep(milliseconds);
    }

    fn CurrentMs(): uint64 {
        return _system_time_ms_internal();
    }

    fn CurrentUs(): uint64 {
        return _system_time_us_internal();
    }

    fn CurrentNs(): uint64 {
        return _system_time_ns_internal();
    }
}