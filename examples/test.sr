mod test::tes2;

use std::fs::{ File, Reader, Writer };
use std::io::{ Stream };

external fn printf(format: string): i32;

enum Test {
    HELLO2: "test",
    HELLO: 123,
}

fn main(): i32 {

    let test: i8 = test(1 + (1 * 2) + (1)) ;

    printf("HEllo!")
}