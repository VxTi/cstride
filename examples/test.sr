mod test::tes2;

use std::fs::{ File, Reader, Writer };
use std::io::{ Stream };

extern fn printf(format: string): i32;

fn test(): string {
    return "

    \x1b[32mHello world from \n\x1b[40manother function

    ";
}


fn main(): void {
    printf(test())
}
