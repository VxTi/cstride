mod test::tes2;

extern fn write(fd: i32, buf: string, count: i32): i32;

extern fn printf(format: string): i32;

let stdio_fd: i32 = 2;

mut k: i8 = 0;

fn test(): string {
    return "Hello world from another function";
}

fn main(): void {

    // STDERR FD

    for (let i: i8 = 0; i < 5; i++) {
        printf(test())
    }

    printf("Hello")
}