

extern fn printf(format: string): i32;

extern fn write(fd: i32, input: string, length: i32): i32;

/**
 * HEllo world!
 */
fn another_fn(): string {
    return "\x1b[31mHellooooo\n";
}

fn hello_world(): string {
    return another_fn();
}

fn main(): void {
    let x: i32 = 10;
    // let name: string = "Stride\n";

    for (let i: i8 = 0; i < 10; i++) {
        write(1, hello_world(), 5)
    }
}

