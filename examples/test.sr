mod test::tes2;

use std::fs::{ File, Reader, Writer };
use std::io::{ Stream };

extern fn printf(format: string): i32;

fn test(): i32 {
    return 10 + 10;
}

fn main(): void {

    let val: i8 = test();

    printf("Hello world!")
    return;
}