fn hoisted_function(x: i64): (i64) -> f64 {
    return (y: i64): f64 -> {
        return 0.0D + x + y;
    };
}

fn main(): i32 {

    const yielded_function = hoisted_function(10);
    const result = yielded_function(20);

    printf("Output: %d", result);

    return 0;
}
