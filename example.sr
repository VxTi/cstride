fn ref(x: int64): (int64) -> int64 {
    return (y: int64): int64 -> {
        return 0.0D + x + y;
    };
}

fn main(): int32 {

    const res = ref(10);
    const sec = res(20);

    printf("Output: %d", sec);

    return 0;
}
