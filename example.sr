/*const xyz = (x: int32): (int32) -> (int32) -> int32 -> {
    return (y: int32): (int32) -> int32 -> {
        return (z: int32):  int32 -> {
            return x + y + z;
        };
    };
};

fn main(): int32 {

    const x = xyz(10);

    const y = x(20);

    const a = y(30);

    printf("A: %d\n", a);

    return 0;
}
 */

type Tuple = [ int32, int64 ];

fn main(): int32 {
    const tuple: Tuple = [42, 100000000000];
}