
type Another = int32;
type Test = {
    field: Another;
};

type Ref = Another;
// type Tuple = (int32, int64);

fn test(x: int64): int64 {
    return x + 20;
}

fn main(): int32 {
    const result = Test::{ field: 123 };

    printf("%d\n", result.field);

    return 0;
}