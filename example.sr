struct Test {
    some_field: float32;
}

module IO {
    /** test */
    pub fn println(arg: string, varargs: int32): void
    {
        printf("z: %d", z.some_field);
    }
}

/**
 * Comment
 */
fn test(): void {}

let x = Test::{ some_field: 3.14 };

fn main(): void {
    test();
    println("z: %d", z.some_field);
}
