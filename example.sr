type Another = int32;

type Test = {
    field: Another;
};

type Ref = Another;

fn test(x: int64): int64 {
    return x + 20;
}

fn main(): int32 {
    const result: Another = Test::{ field: 123 };
    printf("%d\n", result.field);
    return 0;
}