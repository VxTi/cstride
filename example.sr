struct Test {
    some_field: float32;
}

module io {
    /** test */
    public fn println(arg: string, varargs: int32): void
    {
        printf(arg, varargs);
    }
}

let x = Test::{ some_field: 3.14 };

fn main(): void {

    io::println("z: %d", x.some_field);
}
