import System::{
    IO::Print,
    IO::Read
};

fn main(): int32 {
    IO::Print("Type something else: "); // Should print "Result: 15"

    while (true) {
        const input = IO::Read(1);

        IO::Print("Typed: %d", input);
    }

    IO::Print("You entered: %s", input);
    return 0;
}