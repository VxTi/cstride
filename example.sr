    module Foo {
        const field = "foo";
    }

    const field = "bar";

        module io {
        fn println(input: string, ...): void {
            printf(input, ...);
        }
    }

    fn main(): void {
        io::println("field: %s", field);
    }